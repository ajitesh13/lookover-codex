import { PlaceholderPage } from "@/components/placeholder/placeholder-page";

export default function ViolationsPage() {
  return (
    <PlaceholderPage
      eyebrow="Violations"
      title="Violation queue"
      body="A staging view for concentrating the most urgent compliance failures in one place. For now, violations remain explorable inside individual trace reports. This page preserves the Lookover-style navigation and gives us a clean extension point for a dedicated queue."
    />
  );
}
