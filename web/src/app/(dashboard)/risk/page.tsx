import { PlaceholderPage } from "@/components/placeholder/placeholder-page";

export default function RiskPage() {
  return (
    <PlaceholderPage
      eyebrow="Risk"
      title="Risk program"
      body="A simple audit-facing landing area for risk posture, trend review, and escalation context. This section is intentionally non-broken and keeps the Lookover-style information architecture visible while the deeper risk workflows are authored."
    />
  );
}
