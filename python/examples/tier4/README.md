# Tier 4 Example Agents

This folder carries Tier 4 demo agents copied in from `/Users/ajitesh/lookover/sdk/python/examples/Tier 4` so the `pre-run` scanner can be demonstrated against realistic multi-agent Python examples inside this repository.

Current example:

- `agent12_supervisor.py`: supervisor-routed multi-agent workflow with LangGraph/LangChain usage, real tools, and a Lookover listener hook.
